# System Design Documentation

## Overview

XAI-Stock is a two-tier web application with a React frontend and a FastAPI backend. The backend manages JWT authentication, prediction orchestration, history storage, and explainability artifacts. The frontend presents dashboard, prediction, history, profile, and visualization pages.

## Architecture diagram

```mermaid
flowchart LR
  U[User] --> F[React Frontend]
  F --> B[FastAPI Backend]
  B --> DB[(SQLite Database)]
  B --> ML[Prediction and XAI Services]
  ML --> M[(Trained Models and Artifacts)]
  B --> API[REST API Responses]
  F --> API
```

## Flowchart

```mermaid
flowchart TD
  A[Open app] --> B[Login or register]
  B --> C{JWT valid?}
  C -- No --> B
  C -- Yes --> D[Load dashboard]
  D --> E[Run prediction]
  E --> F[Save history]
  F --> G[View charts and explanations]
  G --> H[Edit profile or export history]
```

## Sequence diagram

```mermaid
sequenceDiagram
  actor User
  participant Frontend
  participant Backend
  participant DB

  User->>Frontend: Submit login form
  Frontend->>Backend: POST /api/auth/login
  Backend->>DB: Validate user credentials
  DB-->>Backend: User record
  Backend-->>Frontend: JWT token
  Frontend->>Backend: GET /api/dashboard with Bearer token
  Backend->>DB: Load dashboard and history data
  DB-->>Backend: Data rows
  Backend-->>Frontend: Dashboard payload
  User->>Frontend: Request prediction
  Frontend->>Backend: POST /api/predict
  Backend->>DB: Persist prediction history
  Backend-->>Frontend: Prediction result
```

## ER diagram

```mermaid
erDiagram
  USERS ||--o{ PREDICTION_HISTORY : owns
  USERS ||--o{ TRAINING_HISTORY : owns

  USERS {
    int id PK
    string full_name
    string email
    string hashed_password
    boolean is_active
  }

  PREDICTION_HISTORY {
    int id PK
    int user_id FK
    string symbol
    int sample_index
    string model_name
    string predicted_label
    float predicted_probability
    string probabilities
    string explanation_type
    string input_context
    string explanation_payload
    datetime created_at
  }

  TRAINING_HISTORY {
    int id PK
    int user_id FK
    string status
    string model_name
    string best_model_path
    string comparison_table_path
    string metrics
    string symbols
    int row_count
    string notes
    datetime started_at
    datetime finished_at
    datetime created_at
  }
```

## UML diagram

```mermaid
classDiagram
  class AuthContext {
    +user
    +isAuthenticated
    +login()
    +registerAndLogin()
    +updateProfile()
    +deleteAccount()
    +logout()
  }

  class DashboardPage {
    +loadDashboard()
    +switchSymbol()
  }

  class HistoryPage {
    +searchHistory()
    +filterBySymbol()
    +exportHistory()
  }

  class ProfilePage {
    +saveProfile()
    +changePassword()
    +deleteAccount()
  }

  AuthContext --> DashboardPage
  AuthContext --> HistoryPage
  AuthContext --> ProfilePage
```

## Deployment topology

```mermaid
flowchart LR
  Browser --> Nginx[Frontend container]
  Nginx --> FastAPI[Backend container]
  FastAPI --> SQLite[(Persistent volume or local file)]
```

## Key components

- React app handles navigation, responsive layout, and stateful forms.
- FastAPI exposes authenticated REST endpoints.
- SQLite stores users, prediction history, and training history.
- SHAP and LIME files are generated by the backend and served as artifacts.
